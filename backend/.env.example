# ===========================================
# MiniHub Backend Environment Configuration
# ===========================================
# Copy this file to .env and fill in your values
# NEVER commit .env to version control!

# Server Configuration
PORT=5000
NODE_ENV=development
# NODE_ENV options: 'development' or 'production'

# MongoDB Atlas Connection String
# Get this from MongoDB Atlas: https://www.mongodb.com/atlas
# Format: mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>?retryWrites=true&w=majority
MONGO_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/minihub?retryWrites=true&w=majority

# JWT Secret Key
# IMPORTANT: Generate a secure random string (minimum 64 characters)
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# Rotate immediately if this secret is ever exposed!
JWT_SECRET=your_jwt_secret_key_here_minimum_64_characters

# Frontend URL (for CORS in production)
# Set to your deployed frontend URL
FRONTEND_URL=https://your-frontend.vercel.app

# Rate Limiting Configuration (optional - defaults shown)
# RATE_LIMIT_WINDOW_MS=900000
# RATE_LIMIT_MAX_GENERAL=100
# RATE_LIMIT_MAX_AUTH=20

